[
	dead1, // объект взаимодействия
	"Подтвердить гибель", // текст 
	"\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
    "\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
	"((_this distance _target) < 2) && !(_target getVariable ['opoznan', false])", // условие для дальнейшего выполнения кода
	"(_this distance _target) < 2", 
	{},
	{},
	{_target setVariable ["opoznan", true, true], hint "Тело опознано"}, //Код, исполняемые после успешного выполнения 
	{},
	[],
	5, // длительность функции
	0,
	true,
	false
]
	call BIS_fnc_holdActionAdd;
	
	[
	dead2, // объект взаимодействия
	"Подтвердить гибель", // текст 
	"\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
    "\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
	"((_this distance _target) < 2) && !(_target getVariable ['opoznan', false])", // условие для дальнейшего выполнения кода
	"(_this distance _target) < 2", 
	{},
	{},
	{_target setVariable ["opoznan", true, true], hint "Тело опознано"}, //Код, исполняемые после успешного выполнения 
	{},
	[],
	5, // длительность функции
	0,
	true,
	false
]
	call BIS_fnc_holdActionAdd;
	
	[
	dead3, // объект взаимодействия
	"Подтвердить гибель", // текст 
	"\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
    "\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
	"((_this distance _target) < 2) && !(_target getVariable ['opoznan', false])", // условие для дальнейшего выполнения кода
	"(_this distance _target) < 2", 
	{},
	{},
	{_target setVariable ["opoznan", true, true], hint "Тело опознано"}, //Код, исполняемые после успешного выполнения 
	{},
	[],
	5, // длительность функции
	0,
	true,
	false
]
    call BIS_fnc_holdActionAdd;[
	dead4, // объект взаимодействия
	"Подтвердить гибель", // текст 
	"\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
    "\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
	"((_this distance _target) < 2) && !(_target getVariable ['opoznan', false])", // условие для дальнейшего выполнения кода
	"(_this distance _target) < 2", 
	{},
	{},
	{_target setVariable ["opoznan", true, true], hint "Тело опознано"}, //Код, исполняемые после успешного выполнения 
	{},
	[],
	5, // длительность функции
	0,
	true,
	false
]
	call BIS_fnc_holdActionAdd;
	
	[
	dead5, // объект взаимодействия
	"Подтвердить гибель", // текст 
	"\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
    "\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
	"((_this distance _target) < 2) && !(_target getVariable ['opoznan', false])", // условие для дальнейшего выполнения кода
	"(_this distance _target) < 2", 
	{},
	{},
	{_target setVariable ["opoznan", true, true], hint "Тело опознано"}, //Код, исполняемые после успешного выполнения 
	{},
	[],
	5, // длительность функции
	0,
	true,
	false
]
	call BIS_fnc_holdActionAdd;
	
	[
	dead6, // объект взаимодействия
	"Подтвердить гибель", // текст 
	"\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
    "\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
	"((_this distance _target) < 2) && !(_target getVariable ['opoznan', false])", // условие для дальнейшего выполнения кода
	"(_this distance _target) < 2", 
	{},
	{},
	{_target setVariable ["opoznan", true, true], hint "Тело опознано"}, //Код, исполняемые после успешного выполнения 
	{},
	[],
	5, // длительность функции
	0,
	true,
	false
]
    call BIS_fnc_holdActionAdd;[
	dead7, // объект взаимодействия
	"Подтвердить гибель", // текст 
	"\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
    "\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
	"((_this distance _target) < 2) && !(_target getVariable ['opoznan', false])", // условие для дальнейшего выполнения кода
	"(_this distance _target) < 2", 
	{},
	{},
	{_target setVariable ["opoznan", true, true], hint "Тело опознано"}, //Код, исполняемые после успешного выполнения 
	{},
	[],
	5, // длительность функции
	0,
	true,
	false
]
	call BIS_fnc_holdActionAdd;
	
	[
	dead8, // объект взаимодействия
	"Подтвердить гибель", // текст 
	"\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
    "\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
	"((_this distance _target) < 2) && !(_target getVariable ['opoznan', false])", // условие для дальнейшего выполнения кода
	"(_this distance _target) < 2", 
	{},
	{},
	{_target setVariable ["opoznan", true, true], hint "Тело опознано"}, //Код, исполняемые после успешного выполнения 
	{},
	[],
	5, // длительность функции
	0,
	true,
	false
]
	call BIS_fnc_holdActionAdd;
	
	[
	dead9, // объект взаимодействия
	"Подтвердить гибель", // текст 
	"\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
    "\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
	"((_this distance _target) < 2) && !(_target getVariable ['opoznan', false])", // условие для дальнейшего выполнения кода
	"(_this distance _target) < 2", 
	{},
	{},
	{_target setVariable ["opoznan", true, true], hint "Тело опознано"}, //Код, исполняемые после успешного выполнения 
	{},
	[],
	5, // длительность функции
	0,
	true,
	false
]

    call BIS_fnc_holdActionAdd;	
	[
	dead10, // объект взаимодействия
	"Подтвердить гибель", // текст 
	"\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
    "\a3\ui_f\data\IGUI\Cfg\holdactions\holdAction_forceRespawn_ca.paa", // иконка
	"((_this distance _target) < 2) && !(_target getVariable ['opoznan', false])", // условие для дальнейшего выполнения кода
	"(_this distance _target) < 2", 
	{},
	{},
	{_target setVariable ["opoznan", true, true], hint "Тело опознано"}, //Код, исполняемые после успешного выполнения 
	{},
	[],
	5, // длительность функции
	0,
	true,
	false
]
    call BIS_fnc_holdActionAdd;